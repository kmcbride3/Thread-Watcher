import { isValidDiscordId, safeFetch, sanitizeInput } from "./securityUtils";

// Re-export for backward compatibility
export { isValidDiscordId, safeFetch, sanitizeInput as sanitizeString };

// This file is deprecated. Import these functions directly from "./security" instead.
